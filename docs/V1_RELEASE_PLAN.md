# v1.0.0 shipped

`v1.0.0` is already released. The former release-plan document is now frozen as a historical validation record:

- See [`docs/releases/v1.0-validation-record.md`](releases/v1.0-validation-record.md).

This compatibility pointer stays at the old path so existing repository links continue to resolve.
It is not the active roadmap for post-v1 work.
